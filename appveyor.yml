version: 0.1.{build}

skip_tags: true # avoid deploy to GitHub loop 

# enable patching of dot net core/standard *.csproj files with APPVEYOR version
dotnet_csproj:
  patch: true
  file: '**\*.csproj'
  version: '{version}'
  package_version: '{version}'
  assembly_version: '{version}'
  file_version: '{version}'
  informational_version: '{version}'

image: Visual Studio 2019

configuration:
- Release

before_build:
- nuget restore -Verbosity quiet

build:
  verbosity: minimal              # MSBuild verbosity level

after_build:
- dotnet publish -c Release -r win10-x64
# artifacts:
# - path: ShaderPlayer\bin\$(configuration)\ShaderPlayer.exe
#   name: ShaderPlayer

deploy:
# GitHub release
# - provider: GitHub
#   release: $(appveyor_build_version)
#   description: 'Release auto generated by AppVeyor'
#   auth_token:
#     secure: IGRhoLQi/yyC2ej7s79ZRU3PL3NScj4Kxh1Lyydg/CEvD/PbRwIilxmBziEHfwjJ
#   artifact: ShaderPlayer
#   draft: false
#   prerelease: false
